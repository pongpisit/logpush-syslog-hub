{
  "name": "@logpush-syslog-hub/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit && tsc --noEmit -p test/tsconfig.json",
    "types": "wrangler types",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@logpush-syslog-hub/shared": "workspace:*",
    "hono": "^4.6.14",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.6.4",
    "@vitest/coverage-istanbul": "^2.1.9",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^4.108.0"
  }
}
